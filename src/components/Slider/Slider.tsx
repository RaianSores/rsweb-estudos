import React from 'react';

import styles from '../Slider/Slider.module.scss';

const Slider = () => {
    return (
        <div className={styles.sliderContainer}>
            <img src="/proj3.png" alt="Imagem"/>
        </div>
    )
}

export default Slider
